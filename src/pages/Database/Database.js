import React from "react";
import { Grid } from "@mui/material";

const Database = () => {
  return (
    <Grid
      item
      xs={8}
    >
      Database
    </Grid>
  );
};

export default Database;
